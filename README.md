# rex

A regular expression engine, written from scratch in TypeScript.

It compiles a pattern to a tree, then matches a string against that tree. No
library does the work. The point is the engine, not the wrapper around one.

This repository exists to test Quay Krewe end to end: a design, an approved
path of atomised steps, and every step delivered by a session.
